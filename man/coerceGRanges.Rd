% Generated by roxygen2 (4.0.1): do not edit by hand
\name{coerceGRanges}
\alias{coerceGRanges}
\alias{coerce_granges}
\title{Coerce the coverage to a GRanges object for a given sample}
\usage{
coerceGRanges(sample, fullCov, seqlengths = sapply(fullCov, nrow),
  mc.cores = getOption("mc.cores", 1L),
  mc.outfile = Sys.getenv("SGE_STDERR_PATH"), verbose = TRUE)
}
\arguments{
\item{sample}{The name or integer index of the sample of interest to coerce
to a \code{GRanges} object.}

\item{fullCov}{A list where each element is the result from
\link{loadCoverage} used with \code{returnCoverage = TRUE}. Can be generated
using \link{fullCoverage}.}

\item{seqlengths}{A named vector with the sequence lengths of the
chromosomes. This argument is passed to \link[GenomicRanges]{GRanges}.}

\item{mc.cores}{This argument is passed to \link[BiocParallel]{SnowParam}
to define the number of \code{workers}. Use at most one core per chromosome.}

\item{mc.outfile}{This argument is passed to \link[BiocParallel]{SnowParam}
to specify the \code{outfile} for any output from the workers.}

\item{verbose}{If \code{TRUE} basic status updates will be printed along the
way.}
}
\value{
A \link[GenomicRanges]{GRanges} object with \code{score} metadata
vector containing the coverage information for the specified sample. The
ranges reported are only those for regions of the genome with coverage
greater than zero.
}
\description{
Given the output of \link{fullCoverage}, coerce the coverage to a
\link[GenomicRanges]{GRanges} object.
}
\examples{
\dontrun{
}
}
\author{
Leonardo Collado-Torres
}
\seealso{
\link[GenomicRanges]{GRanges}
}

